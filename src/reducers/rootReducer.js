import images from "../data/gallery-images";
import sliderIcons from "../data/slider-icons";

const initState = {
    images,
    sliderIcons
}

const rootReducer = (state = initState, action) => {
    return state;
}

export default rootReducer;